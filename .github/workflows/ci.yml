name: CI

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      # Pinned deliberately: ruff's default rule set is not stable across
      # releases, and an unpinned install turns this job red on unchanged code.
      # Bump this and the pin in pyproject.toml together.
      - run: pip install ruff==0.16.5
      - run: ruff check dsync/
      - run: ruff format --check dsync/

  build:
    name: Build & Install (Python ${{ matrix.python-version }})
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.9", "3.10", "3.11", "3.12"]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
      - name: Install package
        run: pip install -e .
      - name: Verify CLI entry point
        run: dsync --help
